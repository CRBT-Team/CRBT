// import { ButtonInteraction, Modal } from 'discord.js';

// export const showModal = async (modal: Modal, ctx: ButtonInteraction) => {
//   await ctx.client.ws.on('INTERACTION_CREATE', async (data) => {
//     if (data.type === 5) {
//       ctx.client.emit('modalSubmit', new ModalSubmitInteration());
//     }
//   });
// };
